# Properties file with JDBC and JPA settings.

# Properties that control the population of schema and data for a new data source
jdbc.initLocation=classpath:db/mysql/initDB.sql
jdbc.dataLocation=classpath:db/mysql/populateDB.sql

jpa.showSql=true

#-------------------------------------------------------------------------------
# MySQL Settings

jdbc.driverClassName=com.mysql.jdbc.Driver
jdbc.url=jdbc:mysql://127.0.0.1:3306/sushe?useUnicode=true&characterEncoding=utf-8
jdbc.username=root
jdbc.password=hf0321

jdbc.url2=jdbc:mysql://127.0.0.1:3306/ec?useUnicode=true&characterEncoding=utf-8

# Property that determines which database to use with an AbstractJpaVendorAdapter
jpa.database=MYSQL

#pool settings
jdbc.pool.init=5
jdbc.pool.minIdle=5
jdbc.pool.maxActive=30

#jdbc.testSql=SELECT 'x'
jdbc.testSql=SELECT 1
